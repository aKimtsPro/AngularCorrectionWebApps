import { FormatPlatPipe } from './format-plat.pipe';

describe('FormatPlatPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPlatPipe();
    expect(pipe).toBeTruthy();
  });
});
