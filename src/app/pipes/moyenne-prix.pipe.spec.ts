import { MoyennePrixPipe } from './moyenne-prix.pipe';

describe('MoyennePrixPipe', () => {
  it('create an instance', () => {
    const pipe = new MoyennePrixPipe();
    expect(pipe).toBeTruthy();
  });
});
